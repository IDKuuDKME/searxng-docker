services:
  - type: web
    name: searxng
    env: docker
    plan: free
    region: oregon 
    dockerfilePath: ./searxng/Dockerfile
    envVars:
      - key: SEARXNG_SECRET_KEY
        generateValue: true
